import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'oni-cards-online',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
